# Dummy values so modules that construct clients at import time (e.g. Supabase)
# don't throw during `vitest run`. Not used to reach any real service in tests.
NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
